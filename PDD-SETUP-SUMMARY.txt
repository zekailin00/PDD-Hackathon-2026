PDD Setup Complete
==================

CLIs Configured:
  - codex: OAuth/subscription/config credential configured

API Keys Configured:
  (none)

Files Created / Updated:
  - /Users/zekailin00/Git/pdd/PDD-Hackathon-2026/success_python.prompt
  - /Users/zekailin00/Git/pdd/PDD-Hackathon-2026/PDD-SETUP-SUMMARY.txt

QUICK START
-----------
Setup detected an OAuth/subscription/config-backed agentic CLI but no API key was found in your environment.

1) Issue-driven agentic commands (work NOW with OAuth):
   These dispatch through the configured agentic CLI
   (claude/codex/agy/gemini/opencode) as a subprocess and use its
   stored OAuth/subscription/config credential. No API key required.
     pdd generate <issue-url>
     pdd change   <issue-url>
     pdd bug      <issue-url>
     pdd fix      <issue-url>
     pdd test     <issue-url>
     pdd checkup  <issue-url>

2) Direct prompt commands (local route):
   With a Codex (ChatGPT) subscription login these work WITHOUT an API
   key on the LOCAL route (PDD routes them through the chatgpt/ model
   family; set PDD_MODEL_DEFAULT=chatgpt/gpt-5.6-sol). If PDD Cloud is
   configured, also pass --local so the subscription path is used. Other
   providers call litellm directly and need ANTHROPIC_API_KEY /
   OPENAI_API_KEY / GEMINI_API_KEY.
     pdd generate <prompt-file>
     pdd test     <prompt>
     pdd fix      <prompt>
     pdd sync     <prompt-file>
   To enable these, re-run `pdd setup` and add an API key
   (or use the post-setup options menu's "Add a provider").

Learn More:
  - pdd --help
  - https://promptdriven.ai/
  - Discord: https://discord.gg/Yp4RTh8bG7

